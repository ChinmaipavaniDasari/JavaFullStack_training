class A{
     private A(){
    // if we  make the constructor as the private then we cannot inherit and instanciate
    //we able to  inherit and instanciate by using  remaining three(default,public,private) access specifiers.
    }
}